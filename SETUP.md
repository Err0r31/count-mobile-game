# Инструкция по запуску проекта в Expo Go на телефоне

## Быстрый старт

### 1. Настройка бэкенда

1. **Установите зависимости:**
```bash
cd backend
python -m venv venv
```

2. **Активируйте виртуальное окружение:**
   - Windows: `venv\Scripts\activate`
   - Linux/Mac: `source venv/bin/activate`

3. **Установите пакеты:**
```bash
pip install -r requirements.txt
```

4. **Запустите бэкенд:**
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

✅ Бэкенд запущен! Оставьте этот терминал открытым.

### 2. Настройка фронтенда

1. **Узнайте IP адрес вашего компьютера:**
   - Windows: откройте командную строку и выполните `ipconfig`
   - Найдите IPv4 адрес (например, `192.168.1.100`)
   - Mac/Linux: выполните `ifconfig` или `ip addr`

2. **Создайте файл `.env` в корне проекта:**
```bash
cp .env
```

3. **Откройте `.env` и укажите ваш IP адрес:**
```env
EXPO_PUBLIC_DEV_SERVER_IP=192.168.1.100
EXPO_PUBLIC_API_URL=https://your-production-server.com/api
```
   ⚠️ Замените `192.168.1.100` на ваш реальный IP адрес!

4. **Установите зависимости:**
```bash
npm install
```

5. **Запустите Expo:**
```bash
npm start
```

### 3. Подключение телефона

1. **Установите Expo Go на телефон:**
   - [Android: Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - [iOS: App Store](https://apps.apple.com/app/expo-go/id982107779)

2. **Убедитесь, что телефон и компьютер в одной Wi-Fi сети**

3. **Отсканируйте QR код:**
   - В терминале появится QR код
   - Откройте Expo Go на телефоне
   - Нажмите "Scan QR code" и отсканируйте код из терминала

4. **Проверьте подключение:**
   - В консоли Expo вы увидите логи:
   ```
   🔗 API Base URL: http://192.168.1.100:8000/api
   📱 Platform: android
   🖥️  Is Device: true
   ```
